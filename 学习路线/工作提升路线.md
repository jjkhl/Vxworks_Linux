# 目标岗位

![科益虹源](工作提升路线.assets/image-20260206011201335.png)



# 目标能力

把`VxWorks+C/Cpp+Qt+Rpc+运动控制+设备调试经验`逐步扩展为`C/Cpp+Linux+RTOS+网络通信+并发+设备软件架构`

真正问题是：

* VxWorks经验需要提升为Linux C/C++体系
* 实际能解决问题提升为底层原理系统化

                           C/C++
                             │
                 ┌───────────┴───────────┐
                 │                       │
              Linux                    RTOS
                 │                       │
           ┌─────┼─────┐               VxWorks
           │     │     │
         网络   IPC   多线程
           │     │     │
           └─────┼─────┘
                 │
           设备通信/数据采集
                 │
           ┌─────┼──────┐
           │     │      │
          RPC   TCP    RingBuffer
           │
           ↓
       设备软件架构
           │
       ┌───┴────────┐
       │            │
      Qt         工业设备
对于Linux驱动，只需要了解以下：

```
Linux启动过程
U-Boot是什么
Device Tree是什么
字符设备驱动是什么
ioctl是什么
mmap是什么
中断是什么
DMA是什么
```

需要深入的是`并发+通信`

```
VxWorks：Task→MsgQ→Task→Semaphore
Linux：pthread→queue→condition_variable→mutex
进一步：eventfd→epoll→RingBuffer→Producer/Consumer
进一步：TCP/RPC/IPC/Shared Memory
```

## 阶段一：2026/8~2027/1

目标：把Linux C/C++做到独立开发

学习：`Linux+C+++多线程+网络+IPC+GDB`

重点：

* C++补强
  * RAII(重点)
  * 智能指针
  * 移动语义
  * 对象生命周期
  * 拷贝/移动
  * STL
  * lambda
  * 模板
  * 异常安全
* C++并发：主要能解释为什么这个代码会数据竞争/为什么这个atomic还可能有性能问题/为什么release/acquire能保证数据可见性
  * atomic
  * memory_order
  * condition_variable
  * future
  * promise
  * async
  * race condition(重点)
  * deadlock(重点)
  * livelock(重点)
  * starvation(重点)
  * false sharing(重点)
  * cache coherence(重点)
* GDB调试：熟悉常见指令
  * break
  * run
  * continue
  * next
  * step
  * finish
  * print
  * display
  * backtrace
  * frame
  * info threads
  * thread
  * watch
  * bt
  * 分析core dump

### C++基础

#### RAII

Resource Acquisition Is Initialization，资源获取即初始化：把“资源的生命周期”绑定到“对象的生命周期”

* 资源交给对象管理
* 对象创建时获得资源
* 对象销毁时释放资源
* 利用C++作用域机制保证释放

#### 智能指针

* unique_ptr：独占所有权

```cpp
// 创建
unique_ptr<MyClass> p1 = make_unique<MyClass>();
// 所有权转移
unique_ptr<MyClass> p2 = move(p1);
```

* shared_ptr：共享所有权

```cpp
// 创建
shared_ptr<MyClass> p = make_shared<MyClass>();
// 共享
auto p1 = p;
// 手动释放
p.reset();
```

* weak_ptr：不拥有对象，解决shared_ptr循环引用问题

```cpp
if(auto device = weak.lock())
{
    device->run()
}
```

* **`shared_ptr` 的引用计数是线程安全的，但 `shared_ptr` 指向的对象本身并不因此变成线程安全的**

#### 移动语义

移动语义本质：把一个对象持有的资源“转移”给另一个对象，而不是重新复制一份资源

```cpp
auto p1 = make_unique<Device>();
auto p2 = move(p1);
vector<int> v1(10000000);
vector<int> v2 = move(v1);

Buffer(Buffer&& other);//接管other的资源
```

#### 对象生命周期

一个对象从“开始存在”到“彻底销毁”的这段事件，就是生命周期。

```cpp
栈对象：
作用域结束 → 析构 → 生命周期结束

unique_ptr：
unique_ptr 销毁/释放所有权 → 对象销毁

shared_ptr：
最后一个 shared_ptr 消失 → 对象销毁

weak_ptr：
不延长对象生命周期 → lock() 判断并临时获得所有权
```

#### 左值/右值/右值引用

右值引用允许我们识别“这个对象的资源可以被转移”

```cpp

    	  表达式
   			│
    ┌───────┴───────┐
    ↓               ↓
    左值             右值
    │               │
    T&              T&&
    │
    移动语义
    │
    资源转移
    
左值：有身份、能找到具体对象的表达式
int a;
a;//左值

右值：临时产生、用于提供值的表达式
10;
a+b;
foo();

左值引用：绑定左值
T&
    
右值引用：绑定右值
T&&
    
move：不负责移动，只负责把表达式转换成可以参与移动的右值形式
move(a); // 允许调用T(T&&)
```

#### 拷贝/移动

```CPP
// 拷贝
std::string a = "hello";

std::string b = a;

// 移动
std::string a = "hello";

std::string b = std::move(a);

移动之前：

a ──→ "hello"

移动之后：

a ──→ 空/有效但未指定状态

b ──→ "hello"
    
// 拷贝/移动
class Buffer
{
public:
    Buffer(const Buffer& other);             // 拷贝构造

    Buffer& operator=(const Buffer& other);  // 拷贝赋值
    
    Buffer(Buffer&& other);               // 移动构造

    Buffer& operator=(Buffer&& other);   // 移动赋值
};

// 拷贝构造使用，用一个已经存在的对象，创建一个新对象
Buffer a;
Buffer b = a;

Buffer b(a);

// 拷贝赋值，两个对象已经存在
Buffer a;
Buffer b;
b = a;

// 移动构造
Buffer a;
Buffer b = std::move(a);

// 移动赋值
Buffer a;
Buffer b;
b = std::move(a);
```

#### STL

```cpp
STL
│
├── 容器 Container
│   ├── vector
│   ├── deque
│   ├── list
│   ├── array
│   ├── forward_list
│   ├── map
│   ├── set
│   ├── unordered_map
│   └── unordered_set
│
├── 适配器 Container Adapter
│   ├── stack
│   ├── queue
│   └── priority_queue
│
├── 迭代器 Iterator
│
├── 算法 Algorithm
│   ├── sort
│   ├── find
│   ├── copy
│   ├── remove
│   ├── lower_bound
│   └── ...
│
├── 函数对象 Function Object
│   ├── less
│   ├── greater
│   └── Lambda
│
└── 工具
    ├── pair
    ├── tuple
    ├── make_pair
    └── ...
    
容器
│
├── 顺序容器
│   ├── vector
│   ├── deque
│   ├── list
│   ├── forward_list
│   └── array
│
└── 关联容器
    ├── map
    ├── set
    ├── multimap
    ├── multiset
    ├── unordered_map
    └── unordered_set

// vector，本质动态数组
v.push_back(10);
v.emplace_back(20);

v.pop_back();

v.size();
v.empty();

v[0];
v.at(0);

v.front();
v.back();

v.clear();

v.reserve(100); // 扩容100个空间
v.resize(100); // 填充100个y

| 操作   |  vector |
| ---- | ------: |
| 随机访问 |    O(1) |
| 尾部插入 | 均摊 O(1) |
| 尾部删除 |    O(1) |
| 中间插入 |    O(n) |
| 中间删除 |    O(n) |
| 查找   |    O(n) |

// array，固定大小数组，好处是可以像C++对象一样使用size,begin,end操作
array<int,100> arr;

// deque，双端队列
deque<int> dq;
dq.push_front(10);
dq.push_back(20);

dq.pop_front();
dq.pop_back();

// list，双向链表
list<int> l;
/*
	list虽然插入O(1)，但是节点分散，cache locality差，每个节点都有额外指针，分配次数多，所以很多情况下使用vector
*/

// forward_list，单项链表，相比list节省空间，但是功能更少
forward_list<int> l;

// map，有序key-value
map<int,string> m;
/*
底层通常是红黑树
查找O(logn)
插入O(logn)
删除O(logn)
*/

// set，有序集合
set<int> s;
s.insert(10);
s.insert(20);
s.insert(10);

// 结果：10 20
/*
自动排序，不允许重复
底层通常是树
查找O(logn)
插入O(logn)
删除O(logn)
*/

// multimap/multiset，允许重复

// unordered_map，底层哈希表
unordered_map<int,Device> devices;

key→hash(key)→bucket→value
/*
查找 → O(1)
插入 → O(1)
删除 → O(1)
最坏情况可以退化到O(n)
*/
    
// unordered_set，哈希集合
unordered_set<int> set;

// stack，LIFO(后进先出)，是一种容器适配器，可以把其他容器包装成接口
stack<int> s;
s.push(10);
s.push(20);
s.push(30);

// 30 20 10

// queue，FIFO(先入先出)
queue<int> q;
q.push(10);
q.front();
q.back();
q.pop();

// priority_queue【重点】，默认最大堆(最大元素先出来)，
priority_queue<int> q;
priority_queue<int,vector<int>,less<int>> q;
q.push(3);
q.push(1);
q.top();
q.pop();

// 最小堆
priority_queue<int,vector<int>,greater<int>> q;

// pair，<utility>，两个不同类型的数据打包成一个对象
pair<int,string> p{10,"hello"};
p = {100,"world"};
cout<<p.first<<p.second;

auto p2 = make_pair(20,3.14);
vector<pair<int,int>> vec;
vec.emplace_back(5, 100);
vec.emplace_back(2, 200);
vec.emplace_back(3, 50);
sort(vec.begin(),vec.end(),[](const auto& a, const auto& b){return a.second<b.second});
// {3,50}{5,100}{2,200}
// tuple，固定大小异质集合，任意数量模板参数，2个的时候等同于pair
tuple<int,double,string> t1(1,3.14,"hello");
auto t2 = make_tuple(42,2.71,"world");
tuple<int,double,string> t3;// 默认(0,0.0,"");

int a = std::get<0>(t1); //1
double b = std::get<1>(t1); // 3.14
string c = std::get<2>(t1); // "hello"

string s = std::get<std::string>(t1); // "hello"，要求改类型唯一

// get返回引用，可修改
std::get(0)(t1) = 100;

int x; double y; std::string z;
std::tie(x, y, z) = std::make_tuple(1, 2.0, "three");

// 忽略某些元素
std::tie(x, std::ignore, z) = std::make_tuple(10, 2.0, "abc");

auto t = std::make_tuple(1, 3.14, std::string("hi"));
auto [i, d, s] = t;          // 拷贝
auto& [ri, rd, rs] = t;      // 引用，可修改原值
const auto& [ci, cd, cs] = t; // 只读引用

// option，有值/无值
optional<int> opt1 = 42;//有值
// 无值，不是0也不是-1，是空状态
optional<int> opt2 = std::nullopt;
optional<int> opt3;
cout<<opt1.value();//空的时候调用直接抛异常bad_optional_access
int v2 = *opt1;
int v3 = opt.value_or(0);//有值返回10，空返回0
opt1.reset();//清空

std::optional<BigHeavyObject> obj;
// 满足条件才构造
if(needCreate) {
    obj.emplace(100, "data"); // 就地构造，只在需要时创建
}
// if(opt1.has_value)等价于if(opt1)

// variant，C++17引入的类型安全的联合体，同一时刻只保存一种类型的值，且知道当前保存的是那种类型
variant<int,double,string> v1;// 默认保存第一个类型的值，int(0)

variant<int,double,string> v2 = 3.14;// double
variant<int,double> v3(in_place_index<1>, 3.14); // 指定对象，避免出现歧义，比如3.14隐式转换到第一个类型int
variant<int,double> v4(in_place_type<double>, 3.14);

v2 = 100; //现在保存int
v2 = string("x"); // 现在保存string

int index = v2.index(); // 1(double)
//判断时候保存某类型
if(std::holds_alternative(double)(v))
{
    cout<<"is double"<<endl;
}

std::variant<int, double, std::string> v = std::string("hi");

// 按类型或索引
std::string s = std::get<std::string>(v);
std::string s2 = std::get<2>(v);        // 等价

// get 返回引用，可修改
std::get<std::string>(v) = "world";

// 类型不匹配抛 std::bad_variant_access
// int i = std::get<int>(v);  // 抛异常

// 访问方式
std::variant<int, double, std::string> v = 3.14;
std::visit([](auto&& val) {
    std::cout << val << "\n";
}, v);
std::visit([](auto&& val) {
    using T = std::decay_t<decltype(val)>;
    if constexpr (std::is_same_v<T, int>) {
        std::cout << "int: " << val << "\n";
    } else if constexpr (std::is_same_v<T, double>) {
        std::cout << "double: " << val << "\n";
    } else {
        std::cout << "string: " << val << "\n";
    }
}, v);
// C++17方式使用overloaded
template <class... Ts>
struct overloaded : Ts... { using Ts::operator()...; };
template <class... Ts>
overloaded(Ts...) -> overloaded<Ts...>;   // C++17 推导指引,C++20不再xu

std::visit(overloaded{
    [](int i)         { std::cout << "int " << i; },
    [](double d)      { std::cout << "double " << d; },
    [](const std::string& s) { std::cout << "string " << s; }
    [](auto&& other){ /*兜底，匹配剩下所有类型*/ }
}, v);

// string_view，只读视图，底层只有一个char*指针+size_t长度，不拥有字符内存，不会拷贝字符串数据
std::string s = "abcdef";
std::string_view sv = s;
auto sub = sv.substr(1,3); // "bcd"，零拷贝
/* `size()` / `length()` / `empty()` / `[]` / `at()` / `front()` / `back()` / `data()` / `substr(pos, len)`
重点：`.substr()` O (1)！不会拷贝，只是生成新视图，截取超大文本性能碾压 string::substr。
共同点：都不拥有内存，只是指向外部字符，只读，不会分配堆内存。
核心差异：`string_view`自带长度；`const char*`依靠`'\0'`终止符判断结尾。
*/

```



```cpp
STL Algorithm
│
├── 查找
│   ├── find
│   ├── find_if
│   ├── count
│   ├── count_if
│   ├── binary_search
│   ├── lower_bound
│   └── upper_bound
│
├── 修改
│   ├── copy
│   ├── move
│   ├── fill
│   ├── replace
│   ├── remove
│   └── swap
│
├── 排序
│   ├── sort
│   ├── stable_sort
│   ├── partial_sort
│   ├── nth_element
│   └── is_sorted
│
├── 比较
│   ├── equal
│   ├── mismatch
│   └── lexicographical_compare
│
├── 数值
│   ├── accumulate
│   ├── reduce
│   ├── inner_product
│   └── iota
│
├── 集合算法
│   ├── set_union
│   ├── set_intersection
│   ├── set_difference
│   └── set_symmetric_difference
│
├── 排列
│   ├── next_permutation
│   └── prev_permutation
│
└── 堆
    ├── make_heap
    ├── push_heap
    ├── pop_heap
    └── sort_heap
    
// [begin,end)范围找value
std::vector<int> v = {10, 20, 30, 40};
auto it = std::find(v.begin(), v.end(), 30);
if (it != v.end())
{
    std::cout << "找到: " << *it << std::endl;
}

// 找到满足某个条件的第一个元素
auto it = std::find_if(v.begin(),v.end(),[](int x){return x>20;}); // 30

// 统计某个值出现多少次
int count = std::count(v.begin(),v.end(),2);

// 统计满足条件的元素
int count = std::count_if(v.begin(),v.end(),[](int x){return x>20;}); //2

// 默认升序
sort(v.begin(),v.end());
// 降序
sort(v.begin(),v.end(),std::greater<int>()); 
// a>b返回true表示a要排在b的前面，即表达式返回真表示a在b的左边
sort(v.begin(),v.end(),[](int a,int b){return a>b;}); 
// 注意大小堆lambda比较表示：表达式真表示a排在b的下面
// 排序的同时，保持原来的顺序不变
stable_sort(v.begin(),v.end());

// 反转
reverse(v.begin(),v.end());

// 填充
fille(v.begin(),v.begin()+2,100); //[begin,begin+2)全部设为100

// 替换
replace(v.begin(),v.end(),2,10); // [begin,end)中2全部替换为10

// 复制，目标空间必须提前准备，只负责复制元素
std::vector<int> src = {1, 2, 3, 4};

std::vector<int> dst(4);

std::copy(
    src.begin(),
    src.end(),
    dst.begin()
);

// 移除
auto it = remove(v.begin(),v.end(),3);
// remove不会改变vector的size，只会把不需要得到元素往前搬运

// 条件移除
remove_if(v.begin(),v.end(), [](int x){return x<0;}); // 删除小于0的

// 删除元素
v.erase(it,v.end());

v.erase(remove(v.begin(),v.end(),3), v.end());

// 二分查找，要求数据必须已经有序，复杂度O(logn)
auto it = std::lower_bound(v.begin(),v.end(),30); // 返回第一个大于等于30的位置
auto it = std::upper_bound(v.ebgin(),v.end(),30); // 返回第一个大于30的位置

// 判断一个有序序列中是否存在某个值
bool flag = std::binary_search(v.begin(),v.end(),30);

// 数值算法
int sum = std::accumulate(v.begin(),v.end(),0);// 计算vecotr的所有值的和

int sum = std::accumulate(v.begin(),v.end(), 1, [](int a,int b){return a*b;}); // 计算vector所有值的乘积

// 找最值
auto it = std::min_element(v.begin(),v.end());
auto it = std::max_element(v.begin(),v.end());
auto result = std::minmax_element(v.begin(),v.end());// min = *result.first, max= *result.second

// 对每个元素执行某个操作
std::for_each(v.begin(),v.end(),[](int& x){ x*=2;});

// 对每个元素执行一个转换
std::vector<int> v =
{
    1, 2, 3, 4
};

std::vector<int> result(4);

std::transform(
    v.begin(),
    v.end(),
    result.begin(),
    [](int x)
    {
        return x * 10;
    }
);
/*
v:
1 2 3 4
result:
10 20 30 40
*/

std::vector<int> result;

// back_inserter
std::copy_if(
    v.begin(),
    v.end(),
    std::back_inserter(result), // 每复制一个元素，就调用一下result.push_back()
    [](int x)
    {
        return x > 10;
    }
);

// 数据筛选
vector<int> v = {1,8,3,10,5,20};
auto it = std::partition(v.begin(),v.end(),[](int x){return x<10;}); // 把小于10和大于等于10的分开
1 8 3 5 | 10 20
         ↑
         it
    
// 找到第几小的元素
std::nth_element(v.begin(),v.begin()+2,v.end());
// 执行后v[2]就是第三小的元素，但是前面后面不一定完全排序，主要保证[begin,begin+2)中元素不大于v[2]

```

#### lambda

可以在代码现场定义的匿名函数，把一个很短的函数逻辑直接写在使用它的地方

```cpp
auto func = [](){cout<<"Hello"<<endl;};
/* 捕获列表
	[]：不捕获
	[a,&b]：值捕获a，引用捕获b
	[=]：按值捕获
	[&]：引用捕获
*/
// 初始化捕获
auto p = std::make_unique<int>(100);
auto func =
    [p = std::move(p)]()
    {
        std::cout << *p;
    };
```



#### 模板



#### 异常安全



### C++并发

主要能解释为什么这个代码会数据竞争/为什么这个atomic还可能有性能问题/为什么release/acquire能保证数据可见性

#### atomic



#### memory_order



#### condition_variable



#### future



#### promise



#### async



#### **race condition**



#### **deadlock**



#### **livelock**



#### **starvation**



#### **false sharing**



#### **cache coherence**



### GDB调试

熟悉常见指令

* break
* run
* continue
* next
* step
* finish
* print
* display
* backtrace
* frame
* info threads
* thread
* watch
* bt
* 分析core dump





## 第二阶段：2027/2~2027/7

目标：做一个完整的Linux设备模拟系统

项目：`虚拟运动台控制系统`

```
              Qt上位机
                  │
                TCP
                  │
            Linux控制器
                  │
        ┌─────────┼─────────┐
        │         │         │
     控制线程   数据线程   通信线程
        │         │         │
        └────RingBuffer─────┘
                  │
              模拟设备
                  │
          X/Y/Z运动轴
```

可以实现：模拟三个轴，然后支持move/stop/home/setspeed/setposition/getposition等操作，可以模拟加速度/速度/位置，不需要做到真正的运动控制算法，简单数据模型就行，主要是让软件拥有设备行为

## 第三阶段：2027/7-2027/12

项目：高性能设备数据采集系统

架构：

```
            ┌───────────────┐
            │   Qt客户端    │
            └───────┬───────┘
                    │
                  TCP
                    │
            ┌───────▼───────┐
            │ Linux设备服务 │
            └───────┬───────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
      采集线程     处理线程     网络线程
        │           │           │
        └─────RingBuffer────────┘
                    │
             模拟DMA数据
```

需要自己完成：

* RingBuffer
* Thread Safe Queue
  * 测试多生产者/吞吐量/延迟/CPU/丢包/数据完整性
* 加入Epoll，正好结合VxWorks经验和Linux

```
100个设备连接
        ↓
      epoll
        ↓
   worker线程
        ↓
   device queue
        ↓
   data processor
```

## 第四阶段：2028/1

目标：职业化包装+查漏补缺

```
C++
Linux
GDB
CMake
Git
TCP
RPC
IPC
多线程
RingBuffer
性能分析
Qt
VxWorks
```

## 硬件方面

需要了解以下东西：

```
CPU
RAM
Flash
UART
SPI
I2C
CAN
Ethernet
中断
DMA
```

能看懂简单原理图/datasheet/寄存器手册

# 总体规划

系统层+控制软件+并发+通信

ChatGpt

* https://chatgpt.com/g/g-p-68fb9e2b00bc81919086423ac5b23901-gong-zuo-ti-sheng/shared/c/68fba1ce-d9f4-8322-ba41-571015669aa4?owner_user_id=user-11EIlO4Dk22i3Em5q6tXqT3e

* https://chatgpt.com/g/g-p-68fb9e2b00bc81919086423ac5b23901-gong-zuo-ti-sheng/c/698a0e44-f9a4-8324-942a-16826a3dc311

## 知识点

### RTOS

RTOS：(Read-Time Operating System, 实时操作系统)，比如VxWorks、FreeRTOS(开源)

* 特性
  * 确定性：任务响应时间固定，比如1s内必须完成
  * 任务优先级调度：哪个任务更重要、优先级更高，内核实时切换
  * 轻量级多任务：占用内存少，适合嵌入式环境
  * 无内存分页：直接访问内存，防止延时
  * 任务同步机制：信号量、消息队列、互斥锁等

> VxWorks屏蔽了底层调度器原理，只关注工程使用层，包括创建任务、信号量和消息队列、网络通信和驱动适配、在Workbench里配置BSP；
>
> FreeRTOS可以实现任务调度算法是怎么写的(时间片、优先级)、信号量和消息队列如何实现、上下文切换、Tick中断驱动调度

### BSP

BSP(Board Support Package, 板级支持包)，连接操作系统内核和硬件桥梁

> RTOS内核本身不懂硬件(CPU寄存器、定时器、中断、串口...)
>
> BSP就是告诉RTOS：硬件是这样工作的

一个典型的VxWorks BSP或FreeRTOS移植包，大致包括以下部分

* startup.S/bootrom.c
  * 启动代码，初始化CPU、内存、堆栈，比如上电后第一步执行的汇编
* hwig/
  * 硬件接口层(Timer、UART、Ethernet)，比如初始化中断控制器
* config.h/bsp.h
  * 硬件配置宏定义，比如CPU型号、主频、外设基址
* linker script(.Id)
  * 内存布局搅拌，比如ROM/RAM段划分
* sysLib.c
  * 系统初始化函数(sysHwInit、sysClkInit等)，VxWorks特有
* Makefile
  * 编译该BSP构建规则，比如指定CPU架构、编译选项

```bash
   ┌──────────┐
   │ Bootloader│  ← 由厂商提供（U-Boot、ROM Monitor等）
   └────┬──────┘
        │
        ▼
   ┌──────────┐
   │   BSP    │  ← 初始化CPU/内存/中断/时钟/串口
   └────┬──────┘
        │
        ▼
   ┌──────────┐
   │ RTOS内核 │  ← 调度任务、信号量、驱动注册
   └────┬──────┘
        │
        ▼
   ┌──────────┐
   │ 应用程序 │  ← taskSpawn()/main()
   └──────────┘
```

### 中断和时钟

* 中断：让CPU**立即打断当前任务**，去执行更紧急事件的机制
  * 在RTOS中作用
    * 响应外部事件(如串口收到数据、网络包到达)
    * 触发任务调度(让RTOS根据优先级切换任务)
    * 维持实时性(保证系统对关键事件的快速反应)
* 时钟中断：系统中最固定、最有规律的中断，由一个硬件定时器定期触发，`sysClkConnect、sysClkEnable`
  * 在RTOS中作用
    * 更新系统时间
    * 触发任务延时(vTaskDelay等)
    * 检查定时器任务
    * 实现时间片轮转

## 目标规划

|      阶段      |                      目标                      | 关键字                          | 产出                |
| :------------: | :--------------------------------------------: | ------------------------------- | ------------------- |
|  RTOS内核仿真  |            Linux上模拟小型RTOS内核             | 任务调度/信号量/定时器/消息队列 | `miniRTOS`项目      |
|  任务通信系统  |          实现跨任务通信和RPC通用框架           | socket/RPC/Protobuf/gRPC        | `ninComm`项目       |
| 控制层与上位机 | 模拟多任务控制系统(生产线或机器人)并用Qt可视化 | 并发/数据可视化/状态同步        | Qt界面+后端协同     |
|  系统架构整合  |      形成一个完整的`工业实时系统模拟平台`      | 模块化/多线程调度/实时日志      | `miniIndustrialSim` |

## 总体项目架构图

```bash
+--------------------------------------------------------------+
|                          Qt GUI                              |
|             (状态监控 + 参数配置 + 日志查看)                 |
+---------------------------↑----------------------------------+
|                      miniComm 通信层                         |
|    RPC/gRPC 模拟接口 + 心跳 + 数据共享 + 异步队列             |
+---------------------------↑----------------------------------+
|                       miniRTOS 内核层                        |
|   任务管理 | 调度器 | 信号量 | 消息队列 | 时间片 | 内存池     |
+---------------------------↑----------------------------------+
|                   模拟驱动/控制任务                          |
|   模拟电机 / 传感器 / 控制逻辑 / 异步数据刷新               |
+--------------------------------------------------------------+
```

### 学习内容与价值

| 能力           | 内容                     | 实际价值             |
| -------------- | ------------------------ | -------------------- |
| 实时调度       | 模拟任务优先级、时间片   | 掌握RTOS调度机制     |
| 并发编程       | 信号量、消息队列、任务池 | 提升并发编程能力     |
| 通信框架设计   | RPC/gRPC/PubSub          | 掌握分布式通信思想   |
| 系统框架设计   | 模块化+解耦+分层         | 学习工业控制软件架构 |
| 上位机集成     | Qt+实时数据同步          | 提升完整系统能力     |
| 调试与性能分析 | 日志、延迟、统计分析     | 符合工程开发流程     |

**工业控制系统仿真平台（软件模拟）**

- 独立设计并实现 RTOS-like 内核（任务调度、信号量、队列）
- 构建任务通信框架（基于 gRPC 异步通信）
- 设计 Qt 上位机，实时显示任务状态与通信延迟
- 支持任务优先级调度、超时检测与心跳机制
- 模拟工业控制系统完整架构，完全在 PC 上运行

# 项目

## miniRTOS(模拟实时内核)

* 目标：实现“任务调度+信号量+队列”的最小实时系统
* 技术点
  * C++实现线程池(模拟任务)
  * 条件变量/信号量实现任务同步
  * 实时任务模拟"Tick ISR"
  * 每个任务分配优先级+超时检测
* 产出
  * `task_create(taskFunc, priority)`
  * `sem_take/sem_give`
  * `msgQ_send/msgQ_recv`
  * 调度器自动轮转任务

### miniComm(通信框架)

* 实现任务间通信层

* 技术点

  * gRPC(或自定义轻量RPC)
  * Protobuf定义任务间消息格式
  * 实现发布/订阅机制(pubsub)
  * 实现TCP keep-alive、重连、心跳

* 产出

  * `ServiceRegister`模块(管理服务)
  * `TaskComm`类(封装发送/接收接口)
  * 异步RPC调用机制
  * 日志与状态上报

### Qt上位机(系统监控)

* 目标：Qt界面实时显示RTOS系统的任务状态与通信数据
* 技术点
  * QTableWidget显示任务信息(名称、优先级、状态)
  * QCustomPlot/QChar绘制实时波形(CPU、通信延迟)
  * QTimer+异步刷新机制
  * 通过socket或gRPC调取数据
* 产出
  * 实时任务状态界面
  * 通信性能监控曲线
  * 参数设置窗口(修改调度周期、优先级)

### miniIndustrialSim(整合)

* 目标：将RTOS内核、通信层、Qt界面整合为一个完整可运行系统
* 模拟场景示例(机器人生产线模拟)
  * TaskA：读取传感器数据(周期任务)
  * TaskB：计算控制量(高优先级任务)
  * TaskC：发送命令给执行器
  * miniComm：模拟外部通信(gRPC Client)
  * Qt：显示每个任务运行时间、消息队列深度、延迟
* 最终输出效果(Qt)
  * 各任务状态灯闪烁
  * 任务间消息延迟曲线
  * 系统CPU占用率模拟
  * 死锁检测、优先级反转实验按钮

## 高频数据采集+实时绘图

**高频数据采集 + 实时绘图**

- 1~5kHz
- 丢包 / 抖动
- Qt 绘图不卡

### 采集进程完整最小代码骨架

#### 内存序(memory_order)

c++11定义在`atomic.h`中

`背景`：多线程程序中，有三个层面会影响指令顺序

* 编译器优化重排
* CPU指令乱序执行
* 多核缓存一致性协议

默认下，CPU可以改变指令执行顺序，只要单线程语义不变，但是多线程中顺序十分重要。

C++内存模型允许告诉编译器：我对这个原子操作的顺序要求有多严格。

举个例子：

```cpp
std::atomic<int> flag = 0;
int data = 0;

Thread A:
data = 42;
flag.store(1, std::memory_order_relaxed);

Thread B:
if(flag.load(std::memory_order_relaxed))
    std::cout << data;

/*
 上述代码结果不一定打印42
 因为CPU可能重排为：
 	flag = 1
 	data = 42
 或者ThreadA在Core0，ThreadB在Core1，即使执行顺序不变
 	data = 42
 	flag = 1
 实际发生
 	1. data=42进入Core0的store buffer
 	2. flag=1进入Core0的store buffer
 	3. flag先被刷新到共享内存
 	4. data还在store buffer中
 所以线程B可能看到：
 	flag == 1
 	data == 0
*/

// 正确方法
Thread A:
data = 42;
flag.store(1, std::memory_order_release);
// release保证store之前的所有写都不能重排到store之后

Thread B:
// acquire保证load之后的读都不能重排到load之前
if(flag.load(std::memory_order_acquire))
    std::cout << data;

data = 42
↓
release(flag=1)
↓
acquire(flag==1)
↓
读取 data
```

内存序有下面类型：

| 类型    | 强度              | 用途               |
| ------- | ----------------- | ------------------ |
| relaxed | 最弱              | 只保证原子性       |
| acquire | 读屏障            | 保证后续读不被重排 |
| release | 写屏障            | 保证之前写不被重排 |
| acq_rel | acquire + release | 读写               |
| seq_cst | 最强              | 全局顺序一致       |

对应本项目库代码：

```cpp
template <typename T, size_t N>
class RingBuffer
{
public:
    bool push(const T &item) // 生产者
    {
        // 生产者只读自己写指针
        size_t w = write_.load(std::memory_order_relaxed);
        size_t next = (w + 1) % N;

        // acquire确保看到消费者更新后的read_
        // 放置读到旧数据
        if (next == read_.load(std::memory_order_acquire))
        {
            dropped_++;
            return false; // 满了 → 丢弃
        }

        // 写入数据
        buffer_[w] = item;
        // release保证buffer_吸入
        // 对消费者可见
        write_.store(next, std::memory_order_release);
        return true;
    }

    bool pop(T &out) // 消费者
    {
        size_t r = read_.load(std::memory_order_relaxed);

        // acquire确保看到生产者写入的数据
        if (r == write_.load(std::memory_order_acquire))
            return false;

        out = buffer_[r];
        //release更新read_
        read_.store((r + 1) % N, std::memory_order_release);
        return true;
    }

    uint64_t dropped() const { return dropped_; }

private:
    alignas(64) std::atomic<size_t> write_{0}; // 按照64字节对齐，现代 CPU cache line = 64 bytes。避免cache line伪共享
    alignas(64) std::atomic<size_t> read_{0};
    alignas(64) std::atomic<uint64_t> dropped_{0};
    T buffer_[N];
};


生产者线程                     消费者线程
─────────────                 ─────────────
buffer_[w] = item;
write_.store(next, release)  ────→  write_.load(acquire) 成功
                                     ↓
                               buffer_[r] 可以安全读取了
                                     ↓
                           read_.store(..., release)  ────→
                                                              read_.load(acquire) 看到前进
                                                              ↓
                                                        生产者可以覆盖这个槽了
```



#### Linux下设置当前线程为实时调度线程

头文件`pthread.h`和`sched.h`

`SCHED_FIFO`特点：

* 先到先执行
* 不会被普通线程抢占
* 只有更高优先级线程才能打断
* 不时间片轮转(除非显示让出CPU)

普通线程默认`SCHED_OTHER`

```cpp
void set_realtime_priority(int priority)
{
    sched_param param;
    param.sched_priority = priority; //数字越大优先级越高

    if (pthread_setschedparam(
            pthread_self(),
            SCHED_FIFO, // Linux实时调度策略
            &param) != 0)
    {
        perror("Failed to set RT priority");
    }
}
```





### Qt实时绘图线程安全版

### Ring Buffer / DMA模型

描述：“PC上模拟呢一个DMA+Ring Buffer数据采集模型，生产者线程模拟硬件DMA连续写入，消费者线程模拟应用任务读取，并处理了写快读慢导致的数据覆盖问题。”

介绍：高速数据在生产者和消费者之间零拷贝、低阻塞流动的经典设备架构，是一种硬件+软件共同遵守的数据流模型。

环形缓冲区(Ring Buffer)：想象有一个首尾相连的数组

* 写指针走到尾->回到头
* 读指针追着写指针走
* 永不realloc
* 永不移动数据  

```cpp
[ ][ ][ ][ ][ ][ ][ ][ ]
 ↑                 ↑
read              write
```

主要是解决4个痛点：

| 普通队列    | Ring Buffer |
| ----------- | ----------- |
| malloc/free | 固定内存    |
| memcpy      | 原地读写    |
| cache抖动   | cache友好   |
| 容易阻塞    | 可无锁      |

**设备系统最怕抖动**，所以选用Ring Buffer。

DMA(Direct Memory Access) = 硬件直接写内存

* 不经过CPU
* 不走线程
* 不管代码完成度

软件只需要提前分配一块连续内存，告诉DMA起始地址和长度，最后按照规则"读走已经写好的数据"

DMA配Ring Buffer原因：

* DMA写到尾不能停
* 不能频繁换地址

所以就可以把"硬件DMA"映射到PC模拟项目

模拟关系：

| 真实设备    | PC模拟   |
| ----------- | -------- |
| 传感器      | 采集线程 |
| DMA控制器   | 写线程   |
| ISR         | 定时唤醒 |
| Ring Buffer | 固定数组 |
| 应用任务    | 处理线程 |




## 多客户端设备服务

**多客户端设备服务**

- 同时 5~10 个客户端
- 不互相阻塞
- 限流 / 超时

## “伪 RTOS”线程池

**“伪 RTOS”线程池**

- 优先级队列
- 超时检测
- 负载自适应

